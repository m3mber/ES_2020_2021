spring:
  datasource:
    url: jdbc:mysql://localhost:3306/es13_db
    username: root
    password: mysql
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      auto-offset-reset: earliest
      group-id: groupTest
test:
  topic: ESP13_bus_dataTest